import React from 'react';

const Validate = ({query}) => {

    if (query) {
        return (
            <div>

            </div>
        )
    }
    return (
        <div>
            error
        </div>
    );
};

export default Validate;